
#include <iostream>
int main()
{
unsigned short × {65535}; // mayor valor poisble para unsigned short de 16 bits std: :cout « "x era:" « × « 'In';
× = 65536; // 65536 está fuera de nuestro rango std: :cout « "x es ahora:" « x« 'In';
× = 65537; // 65537 está fuera de nuestro rango std :: cout « "xes ahora:" « × « '\n';

    return 0;
}